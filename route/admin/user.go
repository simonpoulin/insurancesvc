package admin

import "github.com/labstack/echo"

func adminUser(g *echo.Group) {
	// group := g.Group("/users")

	// group.GET("", controller.UserList, validator.UserFindRequest)
	// group.GET("/:id", controller.UserDetail, validator.UserCheckExistance)
	// group.PATCH("/:id", controller.UserUpdate, validator.UserCheckExistance, validator.UserUpdate)
	// group.DELETE("/:id", controller.UserDelete, validator.UserCheckExistance)
}
